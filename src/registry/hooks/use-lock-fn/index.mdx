---
title: useLockFn
description: A hook to lock a function until the previous promise is resolved
---

## Installation [#installation]

<Tabs items={['CLI', 'Manual']}>
  <Tab>
    <InstallCLI value='use-lock-fn' />
  </Tab>
  <Tab>
    Copy and paste the following code into your project.
    <RegistrySourceCode value='use-lock-fn' />
  </Tab>
</Tabs>

## API [#api]

```ts
/**
 * A hook to lock a function until the previous promise is resolved
 * @param fn - The function to lock
 * @returns The locked function
 */
export function useLockFn<P extends any[] = any[], V = any>(
  fn: (...args: P) => Promise<V>,
): (...args: P) => Promise<V | undefined>
```
